name: isekaitavern

services:
  bot:
    build:
      context: .
      dockerfile: Dockerfile

    container_name: isekaitavern-bot
    env_file:
      - .env
    restart: unless-stopped
    init: true
